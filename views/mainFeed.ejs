<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>JamMate</title>
</head>
<body>
  <h1>Jam Session Feed</h1>
  <ul>
    <% for(let i = 0; i < posts.length; i++) { %>
      <li><%= posts[i].userId %> <%= posts[i].content %> (<%= posts[i].timestamp %>)</li>
    <% } %>
  </ul>
</body>
</html>