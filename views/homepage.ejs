<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
  <link href="/jammateCSS.css" rel="stylesheet">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
  <title>JamMate</title>
</head>

<body class="container">
    <div class="p-3 mb-2 bg-light">
        <%- include('partials/header', {user: loggedIn}) %>

        <h1 class="card jm-header">Musician Feed
            <% if (loggedIn) { %>
                <%- include('partials/post_form', {user: user}) %>
                <%- include('partials/ad_form', {user: user}) %>

            <% } else {%>
                <div class="btn-group" role="group" aria-label="Basic example">
                    <a class="nav-link" href="/login"><button type="button" class="btn btn-secondary" >Create Post</button></a>
                    <a class="nav-link" href="/login"><button type="button" class="btn btn-secondary" >Create Ad</button></a>
                </div>
            <% } %>

        </h1>

        <div class="container">
            <div class="row" >
                <div class="col-3 jm-ad">
                    <%- include('partials/ad', { ad_data: ads }) %>
                </div>
                <div class="col-9 jm-post">
                    <% if (posts.length > 0 ) { %>
                        <% for(let i = 0; i < Math.min(9, posts.length); i++) { %>
                            <tr>
                            <%- include('partials/post', {user: loggedIn, post_data: posts[i], userData: user}) %>
                            </tr>
                        <% } %>
                    <% } else { %>
                        <%- include('partials/noPosts') %>
                        <% } %>
                </div>
            </div>
        </div>
    </div>
</body>

<footer>
    <%- include('partials/footer') %>
</footer>
</html>